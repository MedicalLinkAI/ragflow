# Override default nginx server block — only ragflow.conf is active
# This file replaces /etc/nginx/conf.d/default.conf inside the container
# to prevent the default "Welcome to nginx" page from conflicting.
